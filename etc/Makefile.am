# $Id$
if PREFIX_IS_PWD
noinst_DATA = softbot.htrmas.conf example.conf mod_httpd.conf
else
dist_sysconf_DATA = softbot.htrmas.conf example.conf mod_httpd.conf \
                    softbot.cygwin.conf
endif
